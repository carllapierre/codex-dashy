services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        env_file:
            - .env
        ports:
            - '127.0.0.1:${HOST_PORT:-8789}:8789'
        volumes:
            - ./data:/app/data
        restart: unless-stopped
