services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        env_file:
            - .env
        ports:
            - '${HOST_PORT:-8789}:8789'
        volumes:
            - ./data:/app/data
        restart: unless-stopped
